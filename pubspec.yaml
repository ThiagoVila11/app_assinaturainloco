name: clientes_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  flutter_spinkit: ^5.2.0
  google_fonts: ^6.1.0
  table_calendar: ^3.0.9
  intl: ^0.18.1
  url_launcher: ^6.2.5
  shared_preferences: ^2.2.2
  badges: ^3.1.2

  # Firebase (compatíveis entre si e com AGP/NDK atuais)
  #firebase_core: ^2.30.0
  #firebase_messaging: ^14.7.16

  # Notificações
  #flutter_local_notifications: ^17.1.2

  # Speech to Text (versão estável com suporte a namespace)
  speech_to_text: ^7.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
